{
  "name": "windy-plugin-snowline-map-100m",
  "version": "0.3.3",
  "description": "Fast Windy Snowline WBZ map with 100 m labelled isolines, 17x11 viewport sampling, and native forecast timing: 1-hourly to 90 h, 3-hourly to 144 h, then 6-hourly.",
  "main": "dist/plugin.min.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && SERVE=false rollup -c && cp package.json dist/",
    "build:win": "rmdir /s /q dist && mkdir dist && set SERVE=false && rollup -c && copy package.json dist\\",
    "start": "rollup -w -c"
  },
  "keywords": [
    "windy",
    "meteorology",
    "snow",
    "wet-bulb",
    "snow-line",
    "snowline"
  ],
  "license": "MIT",
  "dependencies": {
    "@windycom/plugin-devtools": "^3.0.3"
  }
}
