{
  "name": "windy-plugin-snowline-map-100m",
  "version": "0.4.8",
  "description": "ECMWF wet-bulb-zero Snowline proxy to +144 h with Label only and Label + contour modes, persistent Windy map-elevation probes, adaptive sampling, caching and decluttered contours.",
  "main": "dist/plugin.min.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && SERVE=false rollup -c && cp package.json dist/",
    "build:win": "rmdir /s /q dist && mkdir dist && set SERVE=false && rollup -c && copy package.json dist\\",
    "start": "rollup -w -c"
  },
  "keywords": [
    "windy",
    "meteorology",
    "snow",
    "wet-bulb",
    "snow-line",
    "snowline"
  ],
  "license": "MIT",
  "dependencies": {
    "@windycom/plugin-devtools": "^3.0.3"
  }
}
