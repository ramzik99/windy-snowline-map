name: v21 cleanup

on:
  push:
    paths:
      - 'scripts/v21_a11y_cleanup.py'
      - '.github/workflows/v21-cleanup.yml'

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply accessibility cleanup
        run: python scripts/v21_a11y_cleanup.py
      - name: Install dependencies
        run: npm install
      - name: Build plugin
        run: npm run build
      - name: Commit cleanup
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/plugin.svelte src/PlaceSearch.svelte src/SnowlineChart.svelte src/SoundingChart.svelte
          git commit -m "Clean v21 interactions and accessibility"
          git push
